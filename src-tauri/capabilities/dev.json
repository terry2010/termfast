{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dev",
  "description": "Dev-only capability — loaded only in debug builds",
  "windows": ["main"],
  "permissions": [
    "core:webview:allow-internal-toggle-devtools"
  ]
}
